import React from 'react';
import ReactDOM from 'react-dom';
import './style.css';

const elements = (
  <h1 className="testClass">welcome to React programming.... </h1>
);

ReactDOM.render(elements, document.getElementById('root'));
